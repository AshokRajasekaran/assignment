const addConnection = require("./addconnection");
const removeConnection = require("./removeconnection");
const searchConnections = require("./searchconnections");

module.exports = {
  addConnection,
  removeConnection,
  searchConnections,
};
